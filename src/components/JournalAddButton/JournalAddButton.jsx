import CardButton from '../CardButton/CardButton';
import './JournalAddButton.css';

function JournalAddButton() {
  return <CardButton className="journal-add">New entry</CardButton>;
}

export default JournalAddButton;
